# A fun welcome message
message("Welcome to Dev AtlasTools package")
# Customise the R prompt that prefixes every command
# (use " " for a blank prompt)
options(prompt = "tools> ")
library(roxygen2)
library(devtools)
